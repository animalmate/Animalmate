# 10. 현황 스냅샷 (2026-08-28 기준)

> **목적**: 코드를 읽지 않은 사람에게 "지금 무엇이 되고 무엇이 안 되는지"를 있는 그대로 전달한다.
> **원칙**: 여기에는 **현재 상태만** 쓴다. *왜* 그렇게 했는지는 `07-DECISIONS.md`, 남은 일은
> `04-TODO.md`, 운영 방법은 `README.md` 에 있다. 옮겨 적으면 셋이 서로 어긋난다
> (2026-07-31 에 40KB 까지 불어난 것을 이 원칙으로 걷어냈다).

---

## 1. 한 줄 판단

300명 규모 대학생 봉사 동아리의 운영 자동화. 네이버 카페는 아카이브로 두고 그 위에
**공지 발행 루프 · RAG 챗봇 · 신입 모집 전 과정**을 얹었다.

**2026-07-31 저녁부터 실사용 중.** 인증·조직관리·F1 발행·F9 모집은 완성되어 돌아간다.
**남은 것은 기능이 아니라 운영 작업**이다 — 팀장단이 가이드북 PDF 를 올리는 것(아래 §5-2).

**33기 신입 모집을 한 바퀴 돌렸다.** 지원자 **203명**, 서류 채점 **203/203 완료**, 면접
**2026-08-22 완료**(조 4개 · 슬롯 42칸 · 배정 182명 / 면접 완료 104 · 불참 73). **이 시스템이
겪은 첫 실제 규모다** — 그전까지 실측해 본 것은 연습용 `테스트` 기수 50명뿐이었다.
규모에 관한 판단은 50이 아니라 203 을 기준으로 한다.

**2026-08-28 현재 `33기 추가모집` 기수가 접수 중이다**(지원자 4명, 전원 `received`).
33기 본 기수의 **최종 결정은 아직 누르지 않았다**(`final_pass`/`final_fail` 0건, 최종 결과 공개도 꺼짐).

| | |
|---|---|
| 배포 | Vercel Hobby(서울 `icn1` 고정) + Supabase 무료 티어 |
| 화면 색 | 저채도 팔레트(2026-08-06 전환 — 채도만 0.65배, 명도 유지라 대비비는 그대로거나 상승) |
| 실카페 게시 | 가동 중(2026-07-24 전환). 최근 게시 = `cafe.naver.com/animalmate2010/32991` |
| 품질 게이트 | typecheck 0 · lint 0 · **단위 683 · 통합 220 · RLS 증명 88**(2026-08-28 CI `62dfc49` 실측, 3잡 전부 통과) · `next build` 통과 |
| 챗봇 품질 | **평가셋 33문항, 통과 32~33/33**(2026-08-28 실측, 결정 152·153). `npm run eval` — 실행마다 한 건쯤 흔들린다(temperature 0.2). 리포트는 `eval/results/latest.md` |
| 마이그레이션 | `0000`~`0035`(0034 = `club_guidebooks` + `guidebook_status.extracting`, 0035 = 그 title 칸 제거. 결정 160) — **운영·테스트 DB 양쪽 0035 까지 적용됨**(2026-09-03 확인). 테스트 DB 는 CI 가 매 실행마다 `db:migrate:test` 로 따라가게 한다 |
| 테이블 | **30개**(2026-09-03 운영 실측 — 0034 `club_guidebooks` 추가), **전부 RLS 활성 + 정책 미부여 = 기본 거부**. RLS 증명 케이스 수 = `1 + 테이블수×3` = **91** 로 자동으로 늘어난다 — 목록을 손으로 관리하지 않으므로, **이 숫자가 안 맞으면 테이블 수부터 세어 본다** |
| DB 사용량 | 24MB / 500MB (2026-07-31 확인) |

---

## 2. 기능별 상태

| 기능 | 상태 |
|---|---|
| **인증** — 가입코드 + 이메일 OTP, 세션 무효화, 레이트리밋 | ✅ 실사용 |
| **조직 관리** — 회원·역할·팀·팀장단, 탈퇴, **1년 미접속 자동 만료** | ✅ 실사용 |
| **F1 공지 발행** — 예약 → 미완성 점검 → 카페 실게시 | ✅ 실사용 |
| **F9 신입 모집** — 공고·지원서·채점·면접·최종 검토·최종 결정·폐기 | ✅ 완성, 실전 투입은 **8월 하순** |
| **캘린더**(`/calendar`) — 총회·MT 등, 챗봇 tool 연동 | ✅ 실사용(2026-08-03 신설. 메뉴 이름 `일정`→`캘린더` 2026-08-04) |
| **F4 챗봇** — 검색·툴·쿼터·핸드오프 | ✅ 가동. 기본 정보 + **사이트 사용 안내 3종**(부원·운영진·회장단, 2026-08-26 적재) 입력됨 |
| **번개 게시판**(`/flash`) — 즉흥 소모임 개최·승인·선착순 신청·1:1 쪽지, 챗봇 tool 연동 | ✅ 구현(2026-09-04, 결정 162). 실사용 전 |
| **가이드북**(`/guidebooks`) — 전체 부원 1칸 + 팀별 칸. PDF 업로드·보기, 팀 것만 봉사 운영 정보 챗봇 반영 | ✅ 구현(팀 2026-08-24 / 전체 2026-09-03, 결정 160) + **버킷·왕복 검증 완료**(2026-08-28). 남은 것은 **실제 가이드북 PDF 의 추출 품질**뿐(§5-2) |
| **F8 총무** (dues/expenses) | ⛔ 미착수 — **무기한 연기**(요청 전 착수 금지) |
| **기록 조회**(`/admin/audit`) — 감사 로그를 회장단이 직접 읽는 화면 | ✅ 구현(2026-08-28, 결정 154) |
| **홈 바로가기** — 구글 드라이브(운영진 이상) · 건의함 · 신고함(전원) | ✅ 구현. 주소는 `/guides` 맨 위에서 회장단이 관리(결정 83·161) |
| 지표 대시보드 | ⬜ 미구현 — **지금은 만들지 않는다**(§5-9) |
| 학기 전환 기능(유임 체크 → 일괄 만료) | ⛔ **불필요해짐**(결정 82 — 활동이 곧 갱신이라 사람이 누를 절차가 없다) |

### F1 발행 — 알아 둘 것

- **예약 큐에 처음 들어오면 `예약, 이렇게 합니다` 슬라이드가 저절로 뜬다**(11장, 2026-08-06 신설).
  새 예약부터 카카오톡 예약 메시지까지 **실제 화면**으로 보여준다. `하루 동안 보지 않기`가 있고,
  닫아도 툴바 `예약 흐름 보기`로 다시 열린다. 원문은 `src/guides/reservation-walkthrough.ts`.
  캡처는 손으로 찍지 말 것 — `npm run build && npx tsx scripts/capture-help-shots.ts` 가
  **테스트 DB의 가짜 예약**으로 22장을 다시 만든다(운영 화면을 찍으면 실명이 커밋된다).

- 카페는 **글쓰기 API 만** 있다. 수정·삭제가 없어 **한번 나간 글은 되돌릴 수 없다**(방어가 두 겹인 이유).
- `code 999`(연속 등록 불가)는 실패가 아니다 — 대기 후 재시도, `retry_count` 증가 없음.
- `publishing` 은 워커 점유 상태. 없으면 같은 글을 다음 워커가 다시 집어 **카페에 중복 게시**된다.
- **봉사가 공지 후 취소되면 예약 큐에서 `봉사 취소 표시`** 를 누른다(2026-07-31 신설). 회차만
  취소로 바뀌어 챗봇·안내에서 빠지고, 카페 글과 발행 기록은 남는다.
- **단톡 공지는 사람이 건다.** 예약 큐의 `카카오톡 공지 예약` 이 문구와 시각(**발행 정시** — 카톡
  예약 메시지가 5분 단위라 +1분은 고를 수 없었다, 2026-08-06 개정)을 주면
  팀장단이 카톡 **예약 메시지**로 등록한다(2026-08-04, 결정 88). 자동 전송 API 는 없다.
  문구에는 **게시판 주소**가 들어간다 — 글 번호는 올라가는 순간 매겨져 미리 알 수 없다.
- `events` enum 은 4단계지만 코드가 실제로 쓰는 값은 `draft`·`canceled` 둘뿐이다.

### F4 챗봇 — 알아 둘 것

- **출처는 어디에도 표시하지 않는다**(2026-07-31, 결정 69). 모델이 본문에 쓰지 못하게 하는 규칙은
  유지(맡기면 지어낸다). `sources` 는 `chat_logs` 에만 남는다.
- 봉사 노출 기준 = **"취소 아님 + 장소 있음"**(결정 24). 발행 여부와 무관하다.
- 툴이 **공지 업로드 시각(`upload`)과 집합 시각(`meetTime`)을 따로** 준다(2026-07-31).
- visibility 필터는 **SQL WHERE 에서 강제**한다(질문자 역할 이하 문서·일정만).
- 임베딩 768차원 강제(3072은 pgvector HNSW 한도 2000 초과).
- **청킹은 헤딩 경계로 자른다 — 제목 없는 `##` 구분선도 경계로 친다**(2026-08-28, 결정 153).
  손으로 쓴 문서는 `##` 를 구분선으로만 쓰는 일이 흔한데, 그걸 못 알아보면 문서 전체가 한 섹션이
  되어 길이로만 잘린 잡탕 조각이 나온다. 그 조각은 어떤 질문과도 어중간하게 닮아 **답이 든 조각이
  검색 하위로 밀린다**(회비가 12위였다). 조각이 크면 검색이 나빠진다.
- ⚠ **청킹·임베딩 규칙을 바꾸면 `npx tsx --conditions=react-server scripts/reindex-docs.ts` 를 돌려야
  한다.** `updateDocument` 는 제목·본문이 **바뀌었을 때만** 재색인해서, 화면에서 저장을 다시 눌러도
  기존 문서는 옛 조각을 그대로 들고 있다. 규칙만 고치면 아무것도 달라지지 않는다.
- `TOP_K` 는 8 이다(2026-08-28에 5에서 올렸다). 넓히면 답을 놓칠 확률이 줄지만 **모델이 핸드오프를
  덜 해 지어낼 위험**이 커진다 — 바꾸려면 `npm run eval` 의 handoff·refuse 문항으로 확인한다.
- **날짜가 있는 것은 문서에 적지 않는다** — `/calendar` 에 넣으면 tool `list_club_schedules` 가
  그때그때 읽는다(2026-08-03, 결정 86). 문서에 남길 것은 회칙·회비·FAQ 처럼 날짜 없는 것뿐이다.
- **답하지 못한 질문은 자동으로 모인다** — `/admin/chatbot` 맨 위 카드 + 주 1회 회장단 메일
  (2026-08-03, 결정 85). 무엇을 채워야 하는지 추측하지 않아도 된다. 질문자는 싣지 않는다.
- **봉사 회차가 0건일 때 "없어요"로 끝내지 않는다**(2026-08-24, 결정 139). 회차 → **팀 가이드북의
  평소 운영 방식** → **기본 안내 문구** 3단으로 내려간다. 기본 문구는 `/admin/chatbot` 에서 회장단이 고친다.
  - 회차가 **있을 때는** 이 안내를 붙이지 않는다(비대칭). 붙이면 정확한 답까지 의심스러워진다.
  - 프롬프트가 **확정 일정과 평소 방식을 섞지 못하게** 막는다 — "보통 토요일"을 "이번 주 토요일"로
    말하면 부원이 헛걸음한다.
  - ⚠ `toolGrounded()` 를 `count > 0` 로 되돌리지 말 것. 그러면 회차 0건이 곧바로 핸드오프로 덮여
    **폴백이 통째로 죽는다**(`src/rag/fallback.test.ts` 가 잡는다).
  - ⚠ **시스템 프롬프트에 새 거절 화법을 넣으면 `isHandoff()` 를 함께 고칠 것**(2026-08-26, 결정 142).
    규칙 12 가 "제가 가진 요약에는 없어요"를 가르치자 모델이 일반 질문에도 응용했고, 두 문자열만
    보던 판정이 그것을 "답한 것"으로 세어 **"답하지 못한 질문" 리포트에서 빠졌다.** CI 간헐 실패로
    드러났지만 실체는 운영 집계 버그였다.

### F9 모집 — 알아 둘 것

- **면접 불참은 면접관(운영진)이 면접 콘솔에서** 표시한다(2026-07-31, 결정 67). 되돌릴 수 있다.
- **모집 공고 편집은 회장단 + `teams.can_edit_notice` 가 켜진 팀**(2026-08-25, 결정 140 — 결정 66 을
  제대로 다시 열었다). 회장단이 **회원 → 팀 관리 → 공고 편집 권한 주기**로 팀 단위로 켠다.
  플래그를 켜기 전까지는 회장단 전용과 동작이 같다(전 팀 기본 false).
  **플래그를 켜면 0번 화면 전부 + 지원자 공개 스위치까지 그 팀이 쓴다**(2026-08-25, 결정 141로 확대).
  켜도 회장단만 하는 것: **기수 삭제**, 그리고 합격 여부를 정하는 행위 — 서류/최종 확정·면접 배정·
  지원자 팀 변경·자료 폐기·CSV export. 경계는 "대외에 나가느냐"가 아니라 **"합격을 정하느냐"**다.
  ⚠ `resultPublic`(최종 결과 공개)이 이 권한에서 가장 무겁다 — 켜면 지원자가 즉시 보고 되돌릴 수 없다.
  ⚠ **팀 이름으로 홍보팀을 알아내는 코드를 다시 만들지 말 것** — 그렇게 만든 `isPRTeamOrPrivileged` 는
  UUID 를 이름처럼 비교해 **항상 false** 였다(결정 66).
- 미채점자가 `received` 로 남아 발표 후 "접수 완료(심사 전)"로 조회되는 것은 **의도된 동작**(결정 40).
- **면접 슬롯을 지우면 배정된 지원자의 배정이 함께 풀린다**(FK set null). 화면이 몇 명인지 경고하고,
  지운 내용은 `audit_logs` 에 `[high]` 로 남는다(2026-08-06, 결정 98).
- 면접관은 **활성 임기의 운영진 이상**만 배정된다(서버 검증). 조 이름 중복도 서버가 409 로 막는다(100·101).
- **지원자가 들어오는 길은 사이트 지원 폼 하나뿐이다.** CSV 업로드 화면은 **폐기했다**
  (2026-08-21, 결정 123). 33기는 구글 폼 응답 CSV 로 받았고 그 경로에 붙어 있던 값 보정
  (전화번호 앞 0·생년월일 표기·CP949·중복쌍·주소 경고, 결정 113~117·120~121)은 **시트를 거쳐서**
  필요했던 것이다. 폼은 서버가 저장 전에 값을 검증한다.
  ⚠ **지원자를 손으로 한 명 추가할 방법은 없다** — 예외가 생기면 마감 스위치를 잠시 꺼서 본인이 넣게 한다.
- **채점을 저장할 때 명단을 다시 받지 않는다**(2026-08-20, 결정 119). 203명 기준 저장 1회가
  844KB → 66KB, 167ms 다. 첫 로드는 여전히 전문 포함 582KB 를 한 번 받는다(운영 실측).
- **면접 배정 화면은 1·2·3 세 단계짜리 한 장이다**(2026-08-21, 결정 124). 옛 슬롯 카드 42장
  목록은 걷어냈다 — 배정 보드와 같은 것을 두 번 그리고 있었다. 슬롯 삭제는 보드의 시간 칸에 있다.
- **최종 결정 대상은 면접에 배정된 사람만이다**(2026-08-21, 결정 125). 서류 탈락·미배정·
  불참은 표에 없고, 표 위에 **숫자로** 뜬다.
- **면접 콘솔은 시간대를 고르기 전에는 비어 있다**(2026-08-21, 결정 128). 왼쪽이 `1 시간대 고르기`
  / `2 지원자 고르기` 두 상자로 나뉘고, 전원을 보려면 `전체 보기`를 누른다. **팀 필터는 없앴다.**
- **면접 콘솔 오른쪽 순서** = 자기소개 · 가치관 및 계기 · **대외활동 / 알바 경험** · 서류 채점 기록 ·
  내 메모(결정 129). 대외활동은 비어 있어도 칸이 남는다.
- **상단 사실 줄은 값 길이에 맞춰 칸을 나눈다**(2026-08-21, 결정 132). 예전에는 다섯 칸을 똑같이
  나누고 한 줄로 잘라서 **학교·학과가 203명 중 104명 잘려 있었다**(넓은 화면에서도 똑같았다 —
  본문이 max-width 로 묶여 있다). 지금은 학교·학과가 4/12칸 + 최대 4줄이라 실측 최대 54자도 다 보인다.
- **면접 콘솔과 최종 결정 사이에 5번 최종 검토 화면(`/recruit/review`)이 있다**(2026-08-23,
  결정 133). 운영진 전원이 보는 화면으로, 면접 본 사람을 팀별로 묶어 면접 점수·지망 팀·면접
  총평을 함께 본다. 최종 결정은 5번 → **6번**으로 밀렸다.
- **최종 검토에서 팀장단이 탈락/다른 팀을 체크해 의견을 남긴다**(2026-08-24, 결정 136). 서류
  점수는 뺐다(2번 화면에 있다). 표시는 **의견이지 결정이 아니다** — 상태·배정 팀을 건드리지
  않고, 합격 여부는 여전히 6번에서 회장단이 정한다.
- **'다른 팀' 표시에 갈 팀을 골라 두면, 그 팀 박스 맨 위에도 다른 색 칸으로 뜬다**(2026-08-24,
  결정 137). "5팀 → 4팀"처럼 원래 팀을 함께 적어, 자기 팀 화면만 보는 팀장도 밖에서 넘어오는
  사람을 놓치지 않는다. 이 칸은 **알림 전용**이라 탈락/다른 팀 체크나 갈 팀 변경이 안 된다 —
  둘 다 그 사람의 원래 팀 박스에서만 한다. 박스 제목 인원수에도 더해진다("11명 (8 + 3)").
  같은 화면에 **전화번호** 칸도 추가했다.
- **6번 최종 결정은 5번 표시를 그대로 확정한다**(2026-08-24, 결정 138). 회장단이 체크박스로
  합격자를 다시 고르던 매트릭스를 걷어냈다 — `drop` → 불합격 / `move` + 갈 팀 → 합격(최종 팀은
  그 팀) / 표시 없음 → 합격으로 계산한 **결과 미리보기 표**와 확정 버튼 하나다. 저장 순서는
  **팀 먼저, 상태 나중**이고 팀 배정이 실패하면 상태는 하나도 안 바꾼다.
  ⚠ **확정을 막는 두 가지**: '다른 팀'인데 갈 팀을 안 고른 **팀 미정**(회장단이 대신 정하면 안
  된다) · 표시도 점수도 없는 **면접 채점 미기록**(자동 합격에서 뺀다). 둘 다 이름까지 적어 준다.
  **권한 경계는 그대로다** — 저장하는 것은 여전히 회장단이고, 바뀐 것은 명단을 손으로 다시
  만들던 입력 방식이다.
- ⚠ **생년월일은 등록 후 어느 화면에서도 못 고친다.** 지원자 수정 API 가 받는 것은
  `status·slotId·interviewLink·nearStation·assignedTeam` 뿐이다.
- 지원자 데이터는 기수 단위 hard delete 로만 폐기. 익명 집계만 잔존.

### 캘린더 — 알아 둘 것

- **날짜 있는 것은 캘린더 한 곳에만 둔다.** 문서 화면 머리말에 그렇게 적어 뒀다 — 문서에도 적으면
  한쪽이 낡고, 챗봇은 캘린더를 그때그때 읽으므로 틀린 날짜를 답하게 된다(2026-08-04).
- **`events`(봉사 회차)와 다른 것이다.** events 는 카페 공지 발행 파이프라인(예약·업로드 시각·정원)에
  묶여 있어 총회를 넣으려면 "정원" 같은 칸을 채워야 한다. 봉사는 예약 화면, 그 밖의 일정은 캘린더다.
- **등록·수정·삭제는 회장단**(`schedule.manage`), **조회는 로그인한 전원**(`/calendar` = `requireActor`,
  2026-08-04 결정 89 로 부원에게 개방). 부원에게는 **부원 공개 일정만** 보인다 — 필터는 라우트의
  역할 검사가 아니라 `allowedVisibilities(actor)` 가 건 SQL WHERE 다.
- 도움말 버튼과 "여기 적으면 부원도 바로 봐요" 안내는 **운영진 이상에게만** 보인다(적는 사람에게
  하는 말이다). "챗봇이 부원 질문에 대신 답해요"라는 문구는 걷어냈다 — 부원이 직접 보게 된 뒤로는 틀린 말이다.
- 공개 범위는 **문서와 같은 등급**(`member|staff|board`, `src/auth/visibility.ts` 공용).
- 시간·장소가 미정이어도 등록된다. 비워 두면 챗봇이 "아직 안 정해졌다"고 답한다.

### 번개 — 알아 둘 것

- **봉사(events)·일정(schedules)과 셋 다 다른 것이다.** 봉사는 카페 공지 파이프라인, 일정은 회장단이
  공표하는 공식 일정(신청 없음), 번개는 부원이 열고 부원이 신청하는 소모임이다. 챗봇 tool 도 셋이 따로다.
- **메시지가 곧 신청이다.** 빈 "신청" 버튼이 없다 — 첫 쪽지가 신청 행을 만들고 그대로 대화가 된다.
- **선착순은 `flash_signups.seq` 가 근거다.** 번개 행을 `FOR UPDATE` 로 잠근 트랜잭션 안에서만 채번한다.
  정원이 차도 신청은 받고 대기 번호를 준다. 앞사람이 취소하면 **자동으로** 확정으로 올라간다
  (자리 배정은 매번 seq 순서로 처음부터 재계산 — 정원 축소도 같은 경로로 맞는다).
- **취소 후 재신청은 새 번호다**(대기 줄 맨 뒤). 옛 번호를 살리면 선착순이 아니게 된다.
- **개최는 부원도 낸다.** 부원 것은 `pending`(운영진 승인 대기, 게시판에 안 보인다), 운영진 이상은 곧바로 `open`.
  승인·거절은 **운영진 전원**이 한다(회장단 전용 아님 — 몰아 두면 주말 번개가 월요일에 열린다).
  **거절에는 사유가 필수.**
- **공동 개최.** 개최자는 `flash_hosts` 표로 여러 명이다. 그래서 `flash.manage` 는 이 리포에서 유일하게
  `Ownership` 을 안 쓰는 액션이고, 역할이 아니라 **개최자 여부**로 판단한다(회장단은 override).
- **쪽지는 당사자와 개최자만 본다 — 회장단도 예외가 아니다.** `getFlashDetail` 이 개최자에게만
  `threads` 를 싣고 `postFlashMessage` 는 `flash.manage` 를 아예 보지 않는다. 통합 테스트가 지킨다.
- **알림은 사이트 안에서만.** 메일을 보내지 않는다(번개 하나에 수십 통이 나가면 아무도 안 읽는다).
  홈 카드와 게시판 목록에 안 읽은 쪽지 배지가 뜬다. 읽음 시각은 **개최자마다·신청자마다** 따로 있다.
- **챗봇 tool `list_flash_meetups`.** `from` 에 과거 날짜를 주면 개최 내역이 된다.
  **개최자·신청자 이름은 tool 결과에 없다**(인원 수만) — 챗봇이 명단에 답하지 않는다는 규칙과 부딪힌다.
- **카페에 나가지 않는다**(규칙 #2 — 카페는 수정·삭제가 안 되는데 번개는 자주 바뀐다).
- 명단은 **이름만** 공개(로그인 전원). 연락처는 어디에도 실리지 않는다.

### 가이드북 — 알아 둘 것

- **칸이 두 종류다**(2026-09-03, 결정 160). 맨 위 **전체 부원 가이드북** 한 칸(동아리 전체가 함께
  보는 한 권, 이름 고정 — 새 기수가 오면 파일만 갈아 끼운다)과 그 아래 **팀별 칸**.
  전체 부원 가이드북은 **챗봇이 읽지 않는다** — 파일로만 둔다.
- **PDF 만 받는다.** PPTX 는 브라우저가 못 연다 — "눌러서 바로 보기"가 다운로드가 된다.
  파워포인트에서 PDF 로 저장해 올린다. **상한 50MB**(2026-09-03에 20MB 에서 올렸다).
  이 값은 고른 것이 아니라 Supabase 무료 플랜 파일 상한과 Gemini PDF 상한이 겹치는 자리다.
- **올리는 사람** — 팀 것은 그 팀 팀장단 + 회장단(`guidebook.manage`), 전체 것은 **회장단만**
  (`clubGuidebook.manage`). 팀원은 보기만 된다. **보기는 부원 포함 전원.**
- **부원에게는 파일 이름·용량·올린 날짜·챗봇 딱지를 보내지 않는다**(2026-09-03, 결정 161).
  응답의 `guidebook.admin` 이 통째로 null 이다 — 화면에서 감추는 것이 아니라 서버가 안 보낸다.
  부원에게 남는 것은 팀 이름, `가이드북 보기`, 그리고 없을 때의 안내 한 줄뿐이다.
- **파일과 챗봇 본문은 별개다.** 추출이 실패해도 파일 보기는 된다(딱지가 `챗봇 미반영`).
- **챗봇에 들어가는 것은 가이드북 전문이 아니라 봉사 운영 정보 요약**이다(여는 요일·주기, 신청 방법,
  공지 방식, 준비물). 요약에 없는 것을 물으면 챗봇이 `/guidebooks` 링크로 안내한다.
- **`내용 확인`에서 저장을 눌러야 챗봇이 읽는다.** 안 누르면 `확인 대기`로 남는다 — 파일만 올라간 상태다.
- **행은 추출보다 먼저 남는다**(`status='extracting'`, 2026-09-03). 함수가 60초에 잘려도 파일 보기는
  되고 손으로 적어 넣을 자리가 있다. 옛 순서에서는 잘리면 행이 없어 고아 파일만 쌓였다.
- **추출은 Gemini Files API 로 보낸다**(inlineData 아님). base64 가 부피를 4/3 로 불려
  50MB 짜리가 67MB 문자열 + JSON 사본이 되던 것을 없앴다.
- 글자를 못 찾았어도(이미지 슬라이드) **검수 상자에 직접 적어 넣으면** 챗봇이 답한다.
- 업로드는 **브라우저 → Storage 직접**(Vercel 본문 상한 4.5MB 회피). 버킷은 **비공개**,
  보기는 30분짜리 서명 URL 을 화면 열 때마다 새로 발급한다.
- **비공개 버킷 `team-guidebooks` 는 만들어져 있다**(사용자, 2026-08-28 확인 — `public=false`).
  같은 날 코드 경로 그대로 왕복을 재 봤다(12항목 통과): 서명 업로드 URL → 브라우저 직접 PUT →
  서버가 크기·형식 확인 → **서명 없는 주소로는 400** → 서명 보기 URL 로 열림 → 원본 내려받기 →
  Gemini 추출 → 삭제 후 흔적 없음. 검증은 `team_guidebooks` 행을 만들지 않고 `_qa-verify/` 경로로만 했다 —
  행을 만들면 `/guidebooks` 에 뜨고 챗봇 지식베이스에도 들어간다.

---

## 3. 권한 요약

| 역할 | 들어가는 곳 |
|---|---|
| 비로그인 | `/login` `/signup` `/privacy` `/recruit` `/recruit/notice` `/recruit/apply` `/api/health` |
| 부원 | 홈 · `/chatbot` · `/profile` · **`/calendar`(조회, 부원 공개 일정만)** · **`/guidebooks`(보기)** · **`/flash`(신청·개최 신청)** |
| 운영진(staff+) | + 예약 큐·새 예약·수정 · 템플릿 · 서류 심사 · 면접 콘솔 · **가이드북 업로드(자기 팀 팀장단일 때)** · **번개 개최 승인·거절** |
| 회장단·시스템관리자 | + 회원·팀 · 가입코드 · 게시판 · 챗봇 설정 · 문서 · **일정 등록·수정** · 체크리스트 · 모집 전 단계 |

- **예약 큐는 운영진이 팀과 무관하게 전부 본다.** 고치는 것은 소유자(본인·소속 팀) + 회장단
  (2026-07-31, 결정 64 — 보는 범위와 고치는 범위를 따로 둔다).
- 페이지는 서버 컴포넌트에서 `requireActor`/`requireStaff`/`requireBoard` 로 막는다.
  **모든 쓰기는 서버에서 역할·소유권을 재검증한다** — UI 숨김은 권한이 아니다(규칙 #6).
- 방어선은 두 겹이다: `anon`/`authenticated` 는 public 테이블 권한이 아예 없고(0022 회수),
  그 뒤에 RLS 기본 거부가 있다.

---

## 4. 자동화 · 외부 연동

| 잡 | 주기 | 하는 일 |
|---|---|---|
| pg_cron `publish` (jobid 3) | 매분 | 발행 워커. **할 일이 없으면 audit 을 남기지 않는다**(2026-07-31, 결정 63) |
| pg_cron `draft-generate` (jobid 4) | 매일 | ① D-3/D-1 미완성 점검 ② **1년 미접속** 만료 강등 ③ 레이트리밋 정리 ④ **답하지 못한 질문 리포트(7일마다 회장단 메일)** |
| pg_cron `result-mails` (jobid 5) | 5분마다 | F9 결과 안내 메일 발송 워커. 대기열에서 최대 25통씩, 최근 24시간 400통 한도(Gmail 500통을 인증 메일과 나눠 쓴다). 넘친 것은 다음 날로 |
| Actions `backup.yml` | 주 1회 + 매월 1일 | pg_dump → GPG(AES256) → 비공개 리포. 실패 시 이슈 자동 생성 |
| Actions `keepalive.yml` | — | 60일 비활성으로 스케줄이 꺼지는 것 방지 |

> Vercel Cron 은 **금지**(Hobby 는 하루 1회 + 시각 미보장). `vercel.json` 에 crons 없음.
> 라우트 이름 `draft-generate` 는 옛 기능에서 온 것으로 **하는 일과 다르다**(잡 재등록 회피).

연동: Supabase(DB·Storage·pgvector) / 네이버 카페(글쓰기 전용, 토큰은 DB 에 AES-256-GCM) /
Gemini(생성·임베딩) / Gmail SMTP(**실발송 확인 완료 2026-07-31**) / UptimeRobot 5분 핑.
Sentry 는 미설정.

---

## 5. 지금 열려 있는 것

1. 🟡 **33기 최종 결정이 아직 눌리지 않았다.** 면접(2026-08-22)은 끝났고 `interview_done` 104명 ·
   `interview_noshow` 73명 · `doc_pass` 8명 상태다. `final_pass`/`final_fail` 은 0건이고 최종 결과
   공개 스위치도 꺼져 있다 — 추가모집과 함께 확정하려는 것으로 보인다. **시스템이 재촉할 일은 아니다.**
   6번 화면이 확정을 막는 두 가지(팀 미정 · 면접 채점 미기록)를 이름까지 적어 주므로 그때 걸린다.
   면접에 자리가 없던 서류 합격 3명(결정 127) 건도 그 화면의 `이 표에 없는 사람` 줄에 남아 있다.
2. 🟡 **번개 게시판 — 만들어 놨고, 실제로 쓰이는지가 남았다**(2026-09-04, 결정 162).
   - ✅ 개최·승인·선착순·대기 승격·1:1 쪽지·챗봇 tool 전부 구현. 마이그레이션 0036 적용
     (운영·테스트 DB 양쪽), RLS 증명 103케이스 통과, 통합 테스트 25케이스 통과.
   - ⬜ **아직 아무도 안 써 봤다.** 지금 카톡으로 번개를 여는 사람들이 이쪽으로 옮겨 와야 의미가 있다.
     첫 번개를 여기서 한 번 열어 보고, 카톡 공지에 `신청은 홈페이지 번개 게시판에서` 한 줄을 붙이는
     것이 실제로 필요한 마지막 조각이다(코드가 아니라 사람 쪽 일이다).
   - ⬜ **동시 신청 경합은 실측하지 않았다.** `FOR UPDATE` 로 막아 뒀고 논리는 통합 테스트가
     덮지만, 30명이 같은 순간에 누르는 상황은 재현해 보지 않았다. 실전에서 대기 번호가 이상하면
     여기부터 볼 것.
   - ⚠ **회장단 메뉴가 14개가 됐다.** `console-nav.tsx` 주석의 경고가 그대로 유효하다 —
     1280·1366·1440px 에서 이미 잘리고 있었고 한 칸 더 늘었다. 메뉴를 더 늘리기 전에 손볼 것.
3. 🟡 **팀별 가이드북 — 배선은 다 확인했다. 남은 것은 팀장단이 진짜 파일을 올리는 것뿐이다.**
   `/guidebooks` 에서 팀장단·회장단이 PDF 를 올리면 봉사 운영 정보가 챗봇에 반영된다(결정 139).
   - ✅ **비공개 버킷 `team-guidebooks` 생성 완료**(사용자). 2026-08-28 에 코드 경로 그대로
     왕복 12항목을 확인했다(§2 「팀 가이드북 — 알아 둘 것」). 서명 없는 주소는 400 으로 막힌다.
   - ✅ **Gemini PDF 추출 파이프라인이 동작한다.** 합성 PDF(영문)를 넣으니 한국어 요약으로
     여는 주기·신청 방법·공지 방식·준비물 네 항목을 정확히 뽑았다.
   - ✅ **상한을 50MB 로 올렸다**(2026-09-03, 결정 160). 실제 가이드북이 대부분 20MB 를 넘어
     **한 건도 올리지 못하고 있었다** — 남은 일이 "팀장단이 올리는 것"이었는데 올릴 수가 없었다.
   - ✅ **전체 부원 가이드북 칸을 더했다**(같은 결정). 회장단이 한 권을 올린다(이름 고정, 한 칸).
   - ⬜ **남은 것: 실제 가이드북에서의 추출 품질.** 특히 **글자가 그림인 슬라이드**(PPT 를 그대로
     PDF 로 저장한 경우)는 뽑히지 않을 수 있다. 그때는 검수 상자에 손으로 적어 넣으면 챗봇이
     답한다 — 화면이 이미 그렇게 되어 있다. **합성 PDF 로는 증명되지 않는 부분**이라 실파일이 필요하다.
   - ⬜ **50MB 짜리 실파일의 추출 시간도 아직 안 재 봤다.** `maxDuration=60`(Hobby 상한)이 유일한
     여유 없는 자리다. 잘려도 파일 보기는 되고 손으로 적어 넣을 수 있게 해 뒀지만(§2), 실제로
     몇 초 걸리는지는 실파일이 와야 안다.
   아래는 그 전까지의 기록이다.

   <details><summary>이전 방침(2026-08-03~08-24)</summary>

   **팀별 가이드북 — 제작 중(사용자), 완성되면 링크로 안내한다.**
   "핵심 문서 5개 일괄 입력"은 **보류**(2026-08-03 사용자 결정). 그 사이 전제가 두 번 바뀌었다:
   날짜 있는 것은 문서가 아니라 `/calendar` 로 가고(결정 86), 동아리 기본 정보는 이미 들어가 있다.
   - 가이드북은 **PDF·PPT** 라 마크다운으로 옮기는 비용이 크다 → 본문을 넣는 대신
     **챗봇이 링크를 알려주는 방식**으로 간다(문서 1건에 팀별 링크를 모아 두면 된다).
   - 그때까지의 구멍은 추측하지 말고 `/admin/chatbot` **"답하지 못한 질문"** 카드로 메운다(결정 85).

   </details>
4. ✅ **인프라 계정 소유 이전 — 2026-08-08 에 대부분 해소.** 공용 GitHub 계정 `animalmate1`
   을 만들어 Org owner 로 넣고, Vercel·Supabase 에 걸려 있던 **개인 GitHub 로그인을 정리**했다.
   백업 토큰도 공용 계정으로 재발급해 `backup.yml` 실행으로 왕복을 증명했다(새 백업 커밋 확인).
   - GitHub Org 는 지금 owner 2명(공용 + 개인)이다. **개인 계정 제거는 겨울 인계 때** — 지금
     빼면 개발자가 push 를 못 한다. 목적은 제거가 아니라 이중화였고 그건 달성됐다.
   - Vercel 은 **제거가 아니라 교체**였다. Git 연동이 그 연결을 쓰므로 개인 GitHub 을 빼고
     공용 GitHub 을 다시 연결했다.
   - 남은 개인 의존은 **Gemini 결제 카드 하나**다. 끊겨도 챗봇만 멈춘다(발행·모집·캘린더·
     로그인은 무관). 동아리 카드로 **추가 → 청구 확인 → 개인 카드 제거** 순서로 바꾼다.
   - 상세는 `05-ASSET-REGISTRY.md` 「로그인 수단」.
5. 🟡 **`BACKUP_ENCRYPTION_KEY` 로테이션** — 2026-07-28 작업 중 평문 노출. 사용자가 보류 결정
   (2026-07-31). 실사용 = 진짜 PII 가 백업에 쌓이기 시작하는 시점이라 근거는 남아 있다.
   교체 시 옛 키도 보관해야 한다(기존 백업은 옛 키로만 열린다).
6. ✅ **백업 실패 알림 담당자를 공용 계정으로 옮겼다**(2026-08-08). 리포 변수
   `ALERT_ASSIGNEE = animalmate1`, `backup.yml` 의 기본값도 같은 값으로 바꿨다 —
   변수를 지워도 떠난 개인 계정으로 되돌아가지 않는다.
7. ✅ **평가셋 33문항 작성 + 첫 측정(2026-08-28, 결정 152).** `npm run eval` → `eval/results/latest.md`.
   첫 실행 30/33(91%)에서 **회비·활동 기간이 핸드오프되는 실제 결함**을 잡았고(결정 153),
   고친 뒤 32~33/33(97%)이다. 실행마다 한 건쯤 흔들리므로 **한 번의 숫자를 결론으로 읽지 않는다.**
   ⚠ 문항이 실패하면 코드부터 의심하기 전에 **문항이 틀렸는지** 본다 — 실제로 둘이 그랬다.
8. 🟡 **복원 리허설이 개발자 전용이다.** 절차는 검증됐지만(`05-ASSET-REGISTRY`) `gpg`·`psql`·
   Node 스크립트를 다뤄야 해서 회장단은 실행할 수 없다. 다음 리허설 예정: 2026-10.
9. ✅ **결과 조회 총량 상한을 203명 규모에 맞췄다**(2026-08-20, 결정 118). `recruitLookup` 이
   IP당 **분당 5 → 60**(= 인원 30 × 재시도 2). 옛 값이면 발표 직후 한 공유 IP 뒤 6번째 사람부터
   막혔다. 열거 방어는 이 통이 아니라 `recruitLookupFail`(이름 HMAC, 시간당 10)이 맡으므로
   넓혀도 흔들리지 않는다(결정 80). **배포 완료 — 발표 전에 끝내야 했던 항목이다.**
10. ✅ **기록 조회 화면(`/admin/audit`) 구현(2026-08-28, 결정 154).** 규칙 #4 가 남기는 12,988건을
   회장단이 직접 읽는다. 기본은 사람이 한 일만 보여 준다(87%가 `cron.publish`라 그대로 열면 덮인다).
   - ⬜ **지표 대시보드는 만들지 않기로 했다.** 무엇을 재야 하는지 정해진 적이 없고, 지금 실제로
     보는 숫자는 이미 제자리에 있다 — 발행 성패는 예약 큐 배지, 답 못한 질문은 `/admin/chatbot`
     카드, 챗봇 품질은 평가셋 리포트, 나머지는 기록 화면. 대시보드를 세우면 **같은 사실의 출처가
     하나 더 생겨** 어긋날 자리만 늘어난다. 필요해지면 "무엇을 보고 무엇을 결정할지"부터 정한다.
11. ⬜ 미접속 자동 만료 **실전 검증** — 기준이 1년이라 **2027-07 이후에야** 첫 대상이 생긴다.
   그때 크론 로그·audit(`membership.expire`)을 확인한다.
12. ⬜ 404 페이지 CSP 콘솔 오류(빌드타임 프리렌더라 요청별 nonce 를 못 갖는다). 실사용 영향은 없다.

**추적 중인 버그: 없음.**

---

## 6. 최근에 바뀐 것 (2026-08-27~28)

| 무엇 | 왜 |
|---|---|
| **공개 공고 = 본문이 채워진 최신 기수**(149) | 기수에 발행 상태가 없어 공개 화면이 그냥 최신 기수를 집었다. 회장단이 **다음 기수를 만들기만 해도** 진행 중이던 모집이 내려가고, 지원자가 아무도 안 보는 기수에 접수됐다 — 공격이 아니라 평범한 준비 작업으로 터진다. 후속으로 `btrim` 이 스페이스만 깎아 **줄바꿈만 든 본문**이 발행 신호로 통과하던 것도 고쳤다 |
| **동기화 스크립트가 권한을 실제로 확인**(150) | actor 를 `{ role: 'sysadmin' }` 으로 지어내 넘겨서, 아무 UUID 나 넣어도 문서가 저장되고 감사 로그에 그 사람이 한 것처럼 남았다. 이제 DB 에서 역할을 읽는다. `--audit`(저장된 문서 PII 재검사)·드리프트 표시도 함께 |
| **RAG 문서를 코드와 대조해 고침**(151) | 챗봇이 이 문장들을 그대로 답한다. "PII 는 저장이 막힙니다"(실제로는 경고+그래도 저장), "답 못한 질문은 운영진에게"(실제로는 회장단) 같은 것들 |
| **평가셋 33문항 + 첫 측정**(152) | 그동안 `"실제 문항으로 교체하세요"` 라고 적힌 예시 5개였고 회비를 2만원이라 적어 둔 것까지 있었다(실제 5만원). 돌려도 측정이 아니라 잡음이었다. 첫 실행 91% → 지금 97% |
| 🔴 **회비·활동 기간이 핸드오프되던 검색 결함**(153) | 평가셋이 잡았다. `33기 동아리 기본 정보` 가 `##` 를 **제목 없는 구분선**으로만 써서 문서 전체가 한 섹션이 됐고, 길이로만 잘린 982자 잡탕 조각에 회비가 묻혀 **검색 12위**로 밀렸다. 부원이 가장 많이 묻는 질문이 답 없는 질문이었다. 고친 뒤 조각 2→14개, 회비 **12위→1위** |
| **재색인 수단(`reindex-docs.ts`)** | 청킹을 고쳐도 기존 문서에는 닿지 않는다 — `updateDocument` 는 내용이 바뀌었을 때만 재색인해서 화면에서 저장을 다시 눌러도 소용없다. **규칙만 고치고 도달 수단이 없으면 고친 것이 아니다** |
| **기록 조회 화면(`/admin/audit`)**(154) | 규칙 #4 가 12,988건을 남기는데 읽는 수단이 `psql` 뿐이었다 — 조사할 사람(회장단)과 읽을 수 있는 사람(개발자)이 달랐다. 기본은 사람이 한 일만(87%가 크론) |
| **결과 안내 메일 리허설**(155) | 04-TODO 의 마지막 미체크 항목. 운영의 `테스트` 기수로 돌리려던 계획을 접었다 — 주소가 전부 `@example.invalid` 라 **실패 경로만** 밟으면서 운영에 `failed` 행과 Gmail 반송을 남긴다. 로직은 테스트 DB 통합 9케이스로, 배선은 운영에서 읽기 전용으로(크론 2시간 24회 · HTTP 200) 확인했다 |

> 2026-08-23~26(최종 검토·최종 결정 재설계, 34기 준비 묶음, 결정 133~148)과 그 이전은
> `07-DECISIONS.md` 에 근거째로 남아 있다. 여기서는 걷어냈다 — 세션 로그를 쌓지 않는다.

---

> **갱신 규칙**: 기능이 늘거나 설계가 바뀌면 이 문서의 §2·§3·§5 를 고친다.
> §6 은 다음 큰 작업 때 통째로 교체한다(세션 로그를 쌓지 않는다).
