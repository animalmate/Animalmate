# 10. 현황 스냅샷 (2026-07-31 실사용 개시일 기준)

> **목적**: 코드를 읽지 않은 사람에게 "지금 무엇이 되고 무엇이 안 되는지"를 있는 그대로 전달한다.
> **원칙**: 여기에는 **현재 상태만** 쓴다. *왜* 그렇게 했는지는 `07-DECISIONS.md`, 남은 일은
> `04-TODO.md`, 운영 방법은 `README.md` 에 있다. 옮겨 적으면 셋이 서로 어긋난다
> (2026-07-31 에 40KB 까지 불어난 것을 이 원칙으로 걷어냈다).

---

## 1. 한 줄 판단

300명 규모 대학생 봉사 동아리의 운영 자동화. 네이버 카페는 아카이브로 두고 그 위에
**공지 발행 루프 · RAG 챗봇 · 신입 모집 전 과정**을 얹었다.

**2026-07-31 저녁부터 실사용 중.** 인증·조직관리·F1 발행·F9 모집은 완성되어 돌아간다.
**남은 실질적 공백은 챗봇 지식 문서 하나뿐**이다(아래 §5).

| | |
|---|---|
| 배포 | Vercel Hobby(서울 `icn1` 고정) + Supabase 무료 티어 |
| 실카페 게시 | 가동 중(2026-07-24 전환). 최근 게시 = `cafe.naver.com/animalmate2010/32991` |
| 품질 게이트 | typecheck 0 · lint 0 · **단위 424 · 통합 168** · `next build` 통과 |
| 마이그레이션 | `0000`~`0024` (운영·테스트 DB 모두 적용 완료) |
| 테이블 | 27개, **전부 RLS 활성 + 정책 미부여 = 기본 거부** |
| DB 사용량 | 24MB / 500MB |

---

## 2. 기능별 상태

| 기능 | 상태 |
|---|---|
| **인증** — 가입코드 + 이메일 OTP, 세션 무효화, 레이트리밋 | ✅ 실사용 |
| **조직 관리** — 회원·역할·팀·팀장단, 탈퇴, **1년 미접속 자동 만료** | ✅ 실사용 |
| **F1 공지 발행** — 예약 → 미완성 점검 → 카페 실게시 | ✅ 실사용 |
| **F9 신입 모집** — 공고·지원서·채점·면접·최종 결정·폐기 | ✅ 완성, 실전 투입은 **8월 하순** |
| **F4 챗봇** — 검색·툴·쿼터·핸드오프 | ⚠️ **코드 완성, 지식 문서 2건뿐** |
| **F8 총무** (dues/expenses) | ⛔ 미착수 — **무기한 연기**(요청 전 착수 금지) |
| 학기 전환 기능 / audit 조회 화면 / 지표 대시보드 | ⬜ 미구현 |

### F1 발행 — 알아 둘 것

- 카페는 **글쓰기 API 만** 있다. 수정·삭제가 없어 **한번 나간 글은 되돌릴 수 없다**(방어가 두 겹인 이유).
- `code 999`(연속 등록 불가)는 실패가 아니다 — 대기 후 재시도, `retry_count` 증가 없음.
- `publishing` 은 워커 점유 상태. 없으면 같은 글을 다음 워커가 다시 집어 **카페에 중복 게시**된다.
- **봉사가 공지 후 취소되면 예약 큐에서 `봉사 취소 표시`** 를 누른다(2026-07-31 신설). 회차만
  취소로 바뀌어 챗봇·안내에서 빠지고, 카페 글과 발행 기록은 남는다.
- `events` enum 은 4단계지만 코드가 실제로 쓰는 값은 `draft`·`canceled` 둘뿐이다.

### F4 챗봇 — 알아 둘 것

- **출처는 어디에도 표시하지 않는다**(2026-07-31, 결정 69). 모델이 본문에 쓰지 못하게 하는 규칙은
  유지(맡기면 지어낸다). `sources` 는 `chat_logs` 에만 남는다.
- 봉사 노출 기준 = **"취소 아님 + 장소 있음"**(결정 24). 발행 여부와 무관하다.
- 툴이 **공지 업로드 시각(`upload`)과 집합 시각(`meetTime`)을 따로** 준다(2026-07-31).
- visibility 필터는 **SQL WHERE 에서 강제**한다(질문자 역할 이하 문서만).
- 임베딩 768차원 강제(3072은 pgvector HNSW 한도 2000 초과).

### F9 모집 — 알아 둘 것

- **면접 불참은 면접관(운영진)이 면접 콘솔에서** 표시한다(2026-07-31, 결정 67). 되돌릴 수 있다.
- **모집 공고 설정은 회장단 전용**(2026-07-31, 결정 66 — 홍보팀 개방을 번복).
- 미채점자가 `received` 로 남아 발표 후 "접수 완료(심사 전)"로 조회되는 것은 **의도된 동작**(결정 40).
- 지원자 데이터는 기수 단위 hard delete 로만 폐기. 익명 집계만 잔존.

---

## 3. 권한 요약

| 역할 | 들어가는 곳 |
|---|---|
| 비로그인 | `/login` `/signup` `/privacy` `/recruit` `/recruit/notice` `/recruit/apply` `/api/health` |
| 부원 | 홈 · `/chatbot` · `/profile` |
| 운영진(staff+) | + 예약 큐·새 예약·수정 · 템플릿 · 서류 심사 · 면접 콘솔 |
| 회장단·시스템관리자 | + 회원·팀 · 가입코드 · 게시판 · 챗봇 설정 · 문서 · 체크리스트 · 모집 전 단계 |

- **예약 큐는 운영진이 팀과 무관하게 전부 본다.** 고치는 것은 소유자(본인·소속 팀) + 회장단
  (2026-07-31, 결정 64 — 보는 범위와 고치는 범위를 따로 둔다).
- 페이지는 서버 컴포넌트에서 `requireActor`/`requireStaff`/`requireBoard` 로 막는다.
  **모든 쓰기는 서버에서 역할·소유권을 재검증한다** — UI 숨김은 권한이 아니다(규칙 #6).
- 방어선은 두 겹이다: `anon`/`authenticated` 는 public 테이블 권한이 아예 없고(0022 회수),
  그 뒤에 RLS 기본 거부가 있다.

---

## 4. 자동화 · 외부 연동

| 잡 | 주기 | 하는 일 |
|---|---|---|
| pg_cron `publish` (jobid 3) | 매분 | 발행 워커. **할 일이 없으면 audit 을 남기지 않는다**(2026-07-31, 결정 63) |
| pg_cron `draft-generate` (jobid 4) | 매일 | ① D-3/D-1 미완성 점검 ② 임기 만료 강등 ③ 레이트리밋 정리 |
| Actions `backup.yml` | 주 1회 + 매월 1일 | pg_dump → GPG(AES256) → 비공개 리포. 실패 시 이슈 자동 생성 |
| Actions `keepalive.yml` | — | 60일 비활성으로 스케줄이 꺼지는 것 방지 |

> Vercel Cron 은 **금지**(Hobby 는 하루 1회 + 시각 미보장). `vercel.json` 에 crons 없음.
> 라우트 이름 `draft-generate` 는 옛 기능에서 온 것으로 **하는 일과 다르다**(잡 재등록 회피).

연동: Supabase(DB·Storage·pgvector) / 네이버 카페(글쓰기 전용, 토큰은 DB 에 AES-256-GCM) /
Gemini(생성·임베딩) / Gmail SMTP(**실발송 확인 완료 2026-07-31**) / UptimeRobot 5분 핑.
Sentry 는 미설정.

---

## 5. 지금 열려 있는 것

1. 🔴 **챗봇 지식 문서가 2건뿐이다.** 회칙·봉사 FAQ·회비·봉사시간 인정·연락처를 `/documents` 에
   넣어야 챗봇이 제 몫을 한다. **기능 문제가 아니라 재료 문제다** — 2026-07-31 QA 에서 문서 1건을
   넣자마자 두 섹션을 합쳐 정확히 답하는 것을 확인했다. 넣는 만큼 바로 좋아진다.
2. 🔴 **인프라 계정이 개발자 1명에게만 있다.** GitHub Org(`animalmate`)·메인 리포·백업 리포 모두
   멤버가 `hanchaehun` 한 명이고 그 1명이 owner다. **Vercel 프로젝트 소유 형태는 미확인**
   (URL 이 개인 계정 슬러그 형태라 확인이 필요하다). 이 계정이 사라지면 소스·배포·백업 전부 잠긴다.
   → 겨울 교체 시점에 회장단 계정 초대 예정(사용자 결정).
3. 🟡 **`BACKUP_ENCRYPTION_KEY` 로테이션** — 2026-07-28 작업 중 평문 노출. 사용자가 보류 결정
   (2026-07-31). 실사용 = 진짜 PII 가 백업에 쌓이기 시작하는 시점이라 근거는 남아 있다.
   교체 시 옛 키도 보관해야 한다(기존 백업은 옛 키로만 열린다).
4. 🟡 **백업 실패 알림 담당자가 아직 `hanchaehun`** — 리포 변수 `ALERT_ASSIGNEE`.
   인수인계 때 바꾸지 않으면 떠난 개발자에게 계속 배정된다.
5. 🟡 **평가셋 30문항 미작성.** 오답률·핸드오프율이 측정된 적 없다(`npm run eval` 러너는 있다).
6. 🟡 **복원 리허설이 개발자 전용이다.** 절차는 검증됐지만(`05-ASSET-REGISTRY`) `gpg`·`psql`·
   Node 스크립트를 다뤄야 해서 회장단은 실행할 수 없다. 다음 리허설 예정: 2026-10.
7. ⬜ **결과 조회 총량 상한**(`recruitLookup`, IP당 분당 5회)은 아직 IP 기준이다. 발표 직후 한 IP 뒤
   6명째가 1분 안에 조회하면 막힌다. 실패 카운터만 이름 기준으로 바꿨다(결정 80). 모집 실전 전 판단.
8. ⬜ audit 조회 화면 · 지표 대시보드.
9. ⬜ 미접속 자동 만료 **실전 검증** — 기준이 1년이라 **2027-07 이후에야** 첫 대상이 생긴다.
   그때 크론 로그·audit(`membership.expire`)을 확인한다.
10. ⬜ 404 페이지 CSP 콘솔 오류(빌드타임 프리렌더라 요청별 nonce 를 못 갖는다). 실사용 영향은 없다.

**추적 중인 버그: 없음.**

---

## 6. 최근에 바뀐 것 (2026-07-31 실사용 개시일)

하루 동안 19건. 근거는 전부 `07-DECISIONS.md` 62~83 에 있다.
앞쪽 12건은 실사용 개시 준비, 뒤쪽 7건은 **개시 후 QA·인수인계 점검**에서 나왔다.

| 무엇 | 왜 |
|---|---|
| 가입코드 중복 → 409 | 활성 코드가 0개인데 발급이 정체불명의 500 이었다 = **아무도 가입 못 하는 상태** |
| 크론 audit 소음 제거 | 7일 audit 이 크론 11,543 대 사람 965. 실제로 일한 크론 줄은 1개뿐이었다 |
| 모달 초점 수정 | 팝업 입력창에 한 글자 치면 초점이 튀고 띄어쓰기가 팝업을 닫았다(탈퇴 확인창 2곳 사용 불가) |
| 예약 큐 열람 범위 | 운영진이 남의 팀 공지를 못 봐 겹침을 알아챌 수 없었다 |
| 시각 라벨 24시간 + 새벽 경고 | 업로드 시각을 오전/오후 혼동으로 잘못 지정하는 실수가 실제로 났다 |
| 모집 공고 설정 회장단 전용 | 제목은 "(홍보팀·회장단)"인데 운영진 전원이 들어갔다 |
| 면접 불참 = 면접 콘솔 | 도움말은 있다고 했는데 **입력 수단이 어디에도 없었다** |
| 도움말 전면 재작성 | 설계 근거가 도움말에 섞여 길었다. 하는 법만 남겼다 |
| 챗봇 출처 표시 제거 | 대화가 검색 결과처럼 읽혔다 |
| 발행 후 회차 취소 신설 | 취소된 봉사를 챗봇이 계속 안내하는데 멈출 방법이 없었다 |
| 챗봇 업로드 시각 + 요일 버그 | "몇 시에 올라와?"에 집합 시각으로 답했고, **요일이 하루씩 밀리고 있었다** |
| 봉사 예약 장소 칸 | 장소가 양식에서만 와서, 양식 없이 만들면 반드시 미완성이 됐다 |
| 가입 레이트 리밋 분리 | IP당 시간당 10회 = **한자리에 모여 가입하면 11번째부터 막힘.** 개시 당일 저녁에 터질 결함이었다 |
| 429 화면 처리 | 메일이 안 나갔는데 "코드를 보냈다"며 입력창을 띄워, 오지 않을 코드를 기다리게 했다 |
| 챗봇 질문 PII 서버 거절 | 입력창 안내만 있고 서버는 아무것도 안 봤다. 예정된 이중 방어가 절반만 있었다 |
| 조회 실패 카운터 = 이름 기준 | IP 기준이면 발표 직후 **남의 오타 열 번에 내가 1시간 잠긴다** |
| 면접 출결 권한 구멍 | 운영진이 출결로 `received → 서류합격`을 만들 수 있었다(회장단 게이트 우회) |
| 회장단 상단 메뉴 잘림 | 메뉴 10개가 헤더를 넘겨 1280·1366px 에서 `로그아웃`이 잘렸다 |
| **임기 만료 → 1년 미접속 만료** | 임기 연장 화면이 없어 **2027-01 에 회장단·시스템관리자가 동시 강등**될 예정이었다 |
| 드라이브 링크 설정화 | 기수마다 바뀌는데 코드에 박혀 있어 학기마다 배포가 필요했다 |
| 장애 대응 가이드 신설 | 인수인계에서 가장 비어 있던 자리(`11-INCIDENT-RESPONSE.md`) |

---

> **갱신 규칙**: 기능이 늘거나 설계가 바뀌면 이 문서의 §2·§3·§5 를 고친다.
> §6 은 다음 큰 작업 때 통째로 교체한다(세션 로그를 쌓지 않는다).
